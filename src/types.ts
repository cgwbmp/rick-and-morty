export type Error = {
  code: number,
  message: string,
};

export type Character = {
  id: string,
  name: string,
  image: string,
  status: string,
};

export type Episode = {
  id: string,
  name: string,
  episode: string,
};
